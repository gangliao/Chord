
#include <iostream>
#include <string>

#include "common.h"

namespace chord {
class Node {
   private:
    std::string ip;
    std::uint32_t port;
    byte id[32];

   public:
};
}  // namespace chord